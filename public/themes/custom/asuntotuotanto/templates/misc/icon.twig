{% if icons_path %}
  <svg {% if label %} aria-labelledby="{{ icon }}" {% endif %} class="icon" width="24px" height="24px">
    {% if label %} <title id="{{ icon }}">{{ label|t }}</title>  {% endif %}
    <use xlink:href="{{ icons_path|trim }}#{{ icon }}"/>
  </svg>
{% endif %}
